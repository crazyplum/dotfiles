#!/bin/bash
if [ $(uname -s) == 'GNU/Linux' ]; then
    exec emacsclient --alternate-editor="" -f /home/joe/.emacs.d/server/server -c /sudo::$(readlink -f "$@")
elif [ $(uname -s) == 'Darwin' ]; then
    exec emacsclient --alternate-editor="" -c /sudo::$(readlink -f "$@")
fi

