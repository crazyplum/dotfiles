#!/bin/bash
if [ $(uname -s) == 'GNU/Linux' ]; then
    exec emacsclient --alternate-editor="" -f /home/joe/.emacs.d/server/server -c "$@"
elif [ $(uname -s) == 'Darwin' ]; then
    exec emacsclient --alternate-editor="" -f ~/.emacs.d/server/server -c "$@"
fi
