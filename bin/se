#!/bin/bash

if [ $(uname -s) == 'GNU/Linux' ]; then
    exec emacsclient --alternate-editor="" -f /home/joe/.emacs.d/server/server -n  /sudo::$(readlink -f "$@")
elif [ $(uname -s) == 'Darwin' ]; then
    exec emacsclient --alternate-editor="" -n /sudo::$(readlink -f "$@")
fi

