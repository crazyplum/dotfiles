#!/bin/bash

if [ $(uname -s) == 'GNU/Linux' ]; then
    exec emacsclient --alternate-editor="" -f /home/joe/.emacs.d/server/server /sudo::$(readlink -f "$@")
elif [ $(uname -s) == 'Darwin' ]; then
    exec emacsclient --alternate-editor="" /sudo::$(readlink -f "$@")
fi

