#!/bin/bash
if [ $(uname -o) == 'GNU/Linux' ]; then
	exec emacsclient --alternate-editor="" -f /home/joe/.emacs.d/server/server  "$@"
elif [ $(uname -o) == 'Darwin' ]; then
	exec emacsclient --alternate-editor="" "$@"
fi
